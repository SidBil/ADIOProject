{
  "buildCommand": "npm install && npx expo export --platform web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/images/(.*)", "destination": "/api/index.py" }
  ]
}
